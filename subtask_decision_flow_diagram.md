# Fluxo de Decisão: Sistema de Subtarefas Automáticas

## 🔄 Diagrama de Fluxo Principal

```
┌─────────────────────────────────────────────────────────────────┐
│  USUÁRIO CRIA/ABRE TAREFA                                       │
└─────────────────┬───────────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────────┐
│  SISTEMA ANALISA COMPLEXIDADE                                   │
│  estimateComplexity(title, summary)                             │
└─────────────────┬───────────────────────────────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
    ▼             ▼             ▼
┌────────┐  ┌─────────┐  ┌──────────┐
│ SIMPLES│  │  MÉDIO  │  │ COMPLEXO │
│  ≤3    │  │  4-7    │  │  ≥12     │
│ palavras│  │palavras │  │ palavras │
└───┬────┘  └────┬────┘  └────┬─────┘
    │            │            │
    │            │            │
    ▼            ▼            ▼
┌─────────┐ ┌──────────┐ ┌──────────┐
│  SKIP   │ │ MAX = 3  │ │ MAX = 6  │
│ (array  │ │subtarefas│ │subtarefas│
│  vazio) │ └────┬─────┘ └────┬─────┘
└─────────┘      │            │
    │            └────┬───────┘
    │                 │
    │                 ▼
    │      ┌──────────────────────┐
    │      │  GERAR COM AI        │
    │      │  - Prompt específico │
    │      │  - Temperature 0.3   │
    │      │  - JSON mode         │
    │      └──────────┬───────────┘
    │                 │
    │                 ▼
    │      ┌──────────────────────┐
    │      │  PÓS-PROCESSAMENTO   │
    │      │  - Filtrar triviais  │
    │      │  - Remover duplicatas│
    │      │  - Limitar max       │
    │      └──────────┬───────────┘
    │                 │
    └────────┬────────┘
             │
             ▼
    ┌────────────────┐
    │  RETORNA       │
    │  SUBTAREFAS    │
    │  (0 a 6)       │
    └────────────────┘
```

## 🎯 Árvore de Decisão: Quando Criar Subtarefas

```
Tarefa recebida
    |
    ├─ É Nota ou Ideia?
    |   └─ SIM → ❌ NÃO CRIAR (retorna [])
    |   └─ NÃO → Continue
    |
    ├─ Tem ≤ 3 palavras?
    |   └─ SIM → ❌ NÃO CRIAR (tarefa atômica)
    |   └─ NÃO → Continue
    |
    ├─ Contém padrão SIMPLES?
    |   (pagar conta, enviar email, comprar, lavar)
    |   └─ SIM → ❌ NÃO CRIAR (ação trivial)
    |   └─ NÃO → Continue
    |
    ├─ Usuário forçou (Shift+Click)?
    |   └─ SIM → ✅ CRIAR (override do usuário)
    |   └─ NÃO → Continue
    |
    ├─ Tem ≥ 12 palavras OU padrão COMPLEXO?
    |   (sistema completo, múltiplos, integração)
    |   └─ SIM → ✅ CRIAR até 6 subtarefas
    |   └─ NÃO → Continue
    |
    └─ Tem 4-7 palavras OU padrão MÉDIO?
        (criar, desenvolver, landing page, reunião)
        └─ SIM → ✅ CRIAR até 3 subtarefas
        └─ NÃO → ❌ NÃO CRIAR (fallback conservador)
```

## 📊 Tabela de Padrões e Decisões

| Entrada | Palavras | Padrões | Complexidade | Subtarefas | Razão |
|---------|----------|---------|--------------|------------|-------|
| "Pagar conta luz" | 3 | pagar conta | SIMPLES | 0 | Tarefa atômica |
| "Enviar email cliente" | 3 | enviar email | SIMPLES | 0 | Ação trivial |
| "Comprar leite" | 2 | comprar | SIMPLES | 0 | Muito curta |
| "Criar landing page produto" | 4 | criar, landing page | MÉDIO | 2-3 | Escopo definido |
| "Planejar reunião trimestral" | 3 | reunião | MÉDIO | 2-3 | Tipo específico |
| "Desenvolver dashboard analytics" | 3 | desenvolver, dashboard | MÉDIO | 2-3 | Projeto pequeno |
| "Implementar sistema completo de autenticação com OAuth" | 7 | sistema completo | COMPLEXO | 4-6 | Múltiplas partes |
| "Migrar toda arquitetura de microservices para serverless" | 7 | migração + múltiplos | COMPLEXO | 4-6 | Grande escopo |
| "Ideia: app de meditação" | 4 | tipo=Ideia | N/A | 0 | Não é tarefa |
| "Nota: lembrar de revisar código" | 5 | tipo=Nota | N/A | 0 | Não é tarefa |

## 🤖 Pipeline de Geração AI

```
┌──────────────────────────────────────────────────────────┐
│  INPUT: Tarefa + Complexidade                            │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  FASE 1: Construir Prompt                                │
│  ┌────────────────────────────────────────────────────┐  │
│  │ • Título e descrição da tarefa                     │  │
│  │ • Contexto do tipo (Tarefa, Reunião, etc)         │  │
│  │ • Limite máximo (3 ou 6)                          │  │
│  │ • Regras estritas (7 regras)                      │  │
│  │ • Exemplos de boas/más subtarefas                 │  │
│  │ • Formato JSON esperado                           │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  FASE 2: Chamar AI                                       │
│  ┌────────────────────────────────────────────────────┐  │
│  │ Model: gpt-4o-mini (rápido + barato)             │  │
│  │ Temperature: 0.3 (baixa criatividade)             │  │
│  │ Response Format: JSON object                      │  │
│  │ Max Tokens: 500                                   │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  FASE 3: Validar Resposta                                │
│  ┌────────────────────────────────────────────────────┐  │
│  │ ✓ JSON válido?                                     │  │
│  │ ✓ Campo "subtasks" existe?                        │  │
│  │ ✓ Array de objetos?                               │  │
│  │ ✓ Cada objeto tem "title"?                        │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  FASE 4: Pós-Processamento (CRÍTICO!)                   │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 1. Filtrar Trivialidades                          │  │
│  │    • "Começar", "Finalizar", "Retornar"          │  │
│  │    • "Lavar mãos", "Pegar chave"                 │  │
│  │    • Verbos genéricos sozinhos                   │  │
│  │                                                    │  │
│  │ 2. Remover Duplicatas                             │  │
│  │    • Normalizar texto (lowercase, sem pontuação) │  │
│  │    • Comparar similaridade                       │  │
│  │                                                    │  │
│  │ 3. Limitar Quantidade                             │  │
│  │    • Pegar primeiras N (3 ou 6)                  │  │
│  │                                                    │  │
│  │ 4. Normalizar Posições                            │  │
│  │    • position: 0, 1, 2, ...                      │  │
│  │    • completed: false                            │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  OUTPUT: Array de Subtarefas (0 a 6)                    │
│  [                                                       │
│    { title: "...", position: 0, completed: false },     │
│    { title: "...", position: 1, completed: false }      │
│  ]                                                       │
└──────────────────────────────────────────────────────────┘
```

## ⚖️ Sistema de Pesos: Como o AI Decide

```
ANÁLISE DE COMPLEXIDADE
========================

Tarefa: "Desenvolver sistema de autenticação com OAuth e JWT"

PESOS:
├─ Contagem de palavras: 7 palavras → +10 pontos (médio)
├─ Padrão "desenvolver": +5 pontos
├─ Padrão "sistema": +10 pontos
├─ Múltiplas tecnologias (OAuth, JWT): +15 pontos
└─ TOTAL: 40 pontos

CLASSIFICAÇÃO:
• 0-15 pontos → SIMPLES (não criar)
• 16-35 pontos → MÉDIO (até 3)
• 36+ pontos → COMPLEXO (até 6)

RESULTADO: COMPLEXO → Gerar até 6 subtarefas
```

## 🎨 Exemplos Visuais de Outputs

### ✅ Exemplo BOM - Tarefa Complexa

```
Input: "Criar sistema de agendamento com notificações e integração Google Calendar"

AI Output:
┌────────────────────────────────────────────────────────┐
│ ✓ Criar schema de banco com tabelas de eventos        │
│ ✓ Implementar CRUD de agendamentos                    │
│ ✓ Integrar API do Google Calendar                     │
│ ✓ Implementar sistema de notificações por email       │
│ ✓ Criar interface de visualização de calendário       │
│ ✓ Adicionar testes de integração                      │
└────────────────────────────────────────────────────────┘

Por quê é bom?
✓ Cada subtarefa é específica e acionável
✓ Segue ordem lógica (DB → Backend → Integração → Frontend → Testes)
✓ Não repete o título principal
✓ Verbos de ação claros
✓ Dentro do limite (6)
```

### ❌ Exemplo RUIM (filtrado pelo pós-processamento)

```
Input: "Criar landing page do produto"

AI Output (antes do filtro):
┌────────────────────────────────────────────────────────┐
│ ✗ Começar o projeto                  ← TRIVIAL         │
│ ✗ Criar landing page do produto      ← REPETE TÍTULO   │
│ ✗ Configurar                         ← GENÉRICO        │
│ ✓ Criar wireframe e layout                             │
│ ✓ Implementar seções hero e CTA                        │
│ ✗ Finalizar projeto                  ← TRIVIAL         │
│ ✗ Revisar                            ← GENÉRICO        │
└────────────────────────────────────────────────────────┘

Após Pós-Processamento:
┌────────────────────────────────────────────────────────┐
│ ✓ Criar wireframe e layout                             │
│ ✓ Implementar seções hero e CTA                        │
└────────────────────────────────────────────────────────┘

Por quê foi filtrado?
✓ Removeu "Começar" e "Finalizar" (triviais)
✓ Removeu duplicata do título
✓ Removeu verbos genéricos sem contexto
✓ Manteve apenas subtarefas úteis
```

## 🔒 Regras de Ouro (Nunca Violar)

```
┌───────────────────────────────────────────────────────────┐
│  1. CONSERVADOR POR PADRÃO                               │
│     → Quando em dúvida, NÃO criar                        │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  2. QUALIDADE > QUANTIDADE                               │
│     → 2 subtarefas úteis > 6 genéricas                   │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  3. ACIONÁVEL E ESPECÍFICO                               │
│     → Cada subtarefa deve ter ação clara e contexto      │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  4. RESPEITAR LIMITES                                    │
│     → Médio = 3 max | Complexo = 6 max                   │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  5. FILTRAR TRIVIALIDADES                                │
│     → Pós-processamento é obrigatório                    │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  6. CONTEXTO IMPORTA                                     │
│     → Tipo de tarefa influencia geração                  │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  7. DAR CONTROLE AO USUÁRIO                              │
│     → Sempre permitir override (force flag)              │
└───────────────────────────────────────────────────────────┘
```

## 🧪 Casos de Teste

```typescript
// Caso 1: Tarefa muito simples
test("NÃO cria subtarefas para tarefa atômica", () => {
  const task = { title: "Pagar conta luz", type: "Tarefa" }
  const result = generateSubtasks(task)
  expect(result).toEqual([])
})

// Caso 2: Tarefa média
test("Cria 2-3 subtarefas para tarefa média", async () => {
  const task = { title: "Criar landing page produto", type: "Tarefa" }
  const result = await generateSubtasks(task)
  expect(result.length).toBeGreaterThanOrEqual(2)
  expect(result.length).toBeLessThanOrEqual(3)
})

// Caso 3: Tarefa complexa
test("Cria 4-6 subtarefas para tarefa complexa", async () => {
  const task = {
    title: "Desenvolver sistema completo de autenticação OAuth",
    type: "Tarefa"
  }
  const result = await generateSubtasks(task)
  expect(result.length).toBeGreaterThanOrEqual(4)
  expect(result.length).toBeLessThanOrEqual(6)
})

// Caso 4: Filtra trivialidades
test("Filtra subtarefas triviais", async () => {
  const subtasks = [
    { title: "Começar projeto" },
    { title: "Criar API REST" },
    { title: "Finalizar projeto" }
  ]
  const result = postProcessSubtasks(subtasks)
  expect(result).toHaveLength(1)
  expect(result[0].title).toBe("Criar API REST")
})

// Caso 5: Respeita força
test("Cria subtarefas quando forçado", async () => {
  const task = { title: "Comprar leite", type: "Tarefa" }
  const result = await generateSubtasks(task, { force: true })
  expect(result.length).toBeGreaterThan(0)
})

// Caso 6: Não cria para Notas
test("NÃO cria subtarefas para tipo Nota", async () => {
  const task = { title: "Lembrar de revisar código amanhã", type: "Nota" }
  const result = await generateSubtasks(task)
  expect(result).toEqual([])
})
```

---

**Diagrama criado por**: Claude Code AI Assistant
**Baseado em**: Sistema de subtarefas do zenith-tasks
**Data**: 2025-11-26
**Versão**: 1.0
